version: "3.8"

services:
  usvisa-app:
    build:
      context: .
      dockerfile: DockerFile
    container_name: usvisa-app
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./artifact:/app/artifact
      - ./logs:/app/logs
    restart: unless-stopped
